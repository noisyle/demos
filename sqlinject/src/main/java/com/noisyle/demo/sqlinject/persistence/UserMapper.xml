<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.noisyle.demo.sqlinject.persistence.UserMapper">
    <select id="listUsers" resultType="User" parameterType="User">
        SELECT id, username, password, email FROM demo_user
        where username = '${username}' and password = '${password}'
    </select>
    <select id="listUsersSafe" resultType="User" parameterType="User">
        SELECT id, username, password, email FROM demo_user
        where username = #{username} and password = #{password}
    </select>
</mapper>